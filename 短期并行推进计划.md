# 短期并行推进计划

评估日期：2026-05-25

## 当前口径

本计划基于当前 `main` 分支和正式文档口径制定。

- M0 工程底座已完成：Spring Boot 后端、React/Vite 前端、PostgreSQL Flyway migration、MinIO、Docker Compose、基础测试脚本和 CI 已建立。
- 下一阶段主线是 M1：认证与商品 MVP，覆盖 `G01-G04` 的最小闭环。
- 四人都使用 Codex，开发速度不是主要瓶颈；主要瓶颈是接口契约一致性、代码审查、合并顺序和联调验收。
- 后续开发应优先保证每天都有可演示链路变长，而不是按模块堆半成品。

## M1 冲刺目标

目标日期：2026-05-26

M1 只锁定以下闭环：

```text
注册登录
  -> 提交校园认证
  -> 管理员审核认证通过
  -> 认证学生发布商品
  -> 管理员审核商品通过
  -> 商品进入公开列表、搜索和详情页
```

M1 当天不做以下内容：

- WebSocket 实时聊天。
- 订单、支付、退款、结算。
- AI 发布辅助。
- 复杂推荐。
- 评价信用。
- 举报申诉治理闭环。
- 公网 HTTPS 部署。

## 四人分工

| 成员 | 主线 | 后端交付 | 前端/联调交付 | 建议分支 |
| --- | --- | --- | --- | --- |
| A | 身份、会话、权限 | 注册、登录、退出、当前用户、HttpOnly Cookie、服务端 session、密码哈希、基础权限拦截、角色初始化。 | 提供接口示例和测试账号，协助前端接入登录态。 | `feature/member-auth-session` |
| B | 文件存储与校园认证 | MinIO 上传、文件元数据、认证提交、可信度计算、认证审核、敏感材料访问日志。 | 提供认证提交和审核 API 示例，协助认证页联调。 | `feature/member-campus-verification` |
| C | 商品、分类、审核、搜索 | 商品草稿、图片关联、提交审核、审核上架、商品列表、详情、关键词搜索、分类标签种子数据。 | 提供商品发布、审核、列表、详情 API 示例，协助商品页联调。 | `feature/member-goods-catalog` |
| D | 前端与集成验收 | 补充必要 BFF/接口适配建议，不主写复杂后端。 | 登录页、认证页、商品发布页、商品列表页、商品详情页、管理员审核页、路由守卫、API client、空状态和错误提示。 | `feature/member-m1-frontend` |

## 先合并的契约 PR

正式开发前先做一个小 PR：`feature/m1-contracts`。

该 PR 只做契约，不做业务实现：

- 契约文档：`docs/m1_contracts.md`
- API 路径前缀：
  - `/api/auth/*`
  - `/api/files/*`
  - `/api/verifications/*`
  - `/api/goods/*`
  - `/api/admin/*`
- 统一错误响应格式：
  - `code`
  - `message`
  - `details`
  - `traceId`
- 登录态返回结构：
  - `id`
  - `displayName`
  - `roles`
  - `verificationStatus`
- M1 状态枚举：
  - 用户认证状态。
  - 文件用途和可见范围。
  - 商品状态。
  - 商品审核状态。
- migration 编号分配。
- M1 种子数据边界。

建议 migration 编号：

| 模块 | 编号范围 |
| --- | --- |
| Auth / Session / Roles | `V3-V5` |
| Files / Campus Verification | `V6-V8` |
| Goods / Categories / Audit | `V9-V12` |
| Search / Seeds / Integration Fixes | `V13-V14` |

## 当天合并顺序

1. `feature/m1-contracts`
2. `feature/member-auth-session`
3. `feature/member-campus-verification`
4. `feature/member-goods-catalog`
5. `feature/member-m1-frontend`
6. `feature/m1-integration-polish`

合并原则：

- 小 PR 优先，单个 PR 只解决一个主题。
- 后端 PR 必须带基础测试或可复现验证命令。
- 前端 PR 必须能通过 `npm test -- --run` 和 `npm run build`。
- 每次合并前跑 `./scripts/check.sh`。
- 发现契约问题先改契约，再改实现，避免前后端互相猜。

## 2026-05-26 时间表

| 时间 | 目标 |
| --- | --- |
| 09:00-10:00 | 四人同步契约，完成 `feature/m1-contracts`。 |
| 10:00-13:00 | A/B/C 写后端主干，D 写前端路由、API client 和 mock 页面。 |
| 13:00-14:00 | 第一轮审查与合并：contracts + auth。 |
| 14:00-17:00 | B/C 完成认证和商品主流程，D 接入真实 auth。 |
| 17:00-18:00 | 第二轮审查与合并：files/verification + goods。 |
| 19:00-21:00 | 全链路联调：登录、认证、审核、发商品、审核商品、搜索浏览。 |
| 21:00-23:00 | 只修 bug、补测试、补种子数据、补 README，不加新功能。 |

## M1 验收清单

M1 完成时，至少可以演示：

- 用户注册、登录、退出。
- 登录态通过 HttpOnly Cookie 和服务端 session 维持。
- 用户可以查询当前登录状态。
- 普通用户可以提交校园认证资料。
- 文件二进制进入 MinIO，数据库保存文件元数据。
- 系统计算认证可信度。
- 管理员可以审核认证。
- 认证通过用户可以发布商品草稿并提交审核。
- 管理员可以审核商品。
- 审核通过商品进入公开列表。
- 公开商品支持详情查看和关键词搜索。
- 受保护接口不能被未登录用户直接访问。
- 业务验证和前端构建通过 `./scripts/check.sh`。

## 后续两周推进节奏

如果 M1 在 2026-05-26 完成，后续按以下顺序推进：

| 阶段 | 建议时间 | 目标 | 必须交付 |
| --- | --- | --- | --- |
| M2 | 2 天 | 会话协商 + 订单创建 + 商品占用 | 会话、议价卡片雏形、订单创建、商品行级锁占用、卖家确认、买家完成确认。 |
| M3 | 2-3 天 | 担保支付闭环 | 模拟支付单、支付回调幂等、托管状态、退款申请、结算冻结期、结算记录。 |
| M4 | 2 天 | 信用治理闭环 | 评价、信用记录、举报、管理员处理、申诉雏形、处罚记录。 |
| M5 | 2 天 | 通知、统计、审计与后台补齐 | 站内通知、公告、统计页、操作日志、敏感访问日志查询、管理后台整合。 |
| 收尾 | 2-3 天 | 验收与答辩准备 | 测试补齐、演示数据、README、部署说明、截图、答辩材料、bug bash。 |

## 后续功能取舍

必须优先：

- 身份、认证、商品、订单、支付、评价、举报的主流程闭环。
- 状态机、幂等、审计、权限隔离。
- 可演示页面和可复现数据。

可以降级：

- WebSocket 可以先用轮询或普通接口补偿，最后再升级。
- AI 发布辅助可以保留开关和接口占位，不进入主路径。
- 推荐可以先做规则排序和关键词搜索，不做复杂个性化。
- 支付使用模拟渠道，不接真实支付平台。
- 信用展示先做维度摘要和记录列表，不做复杂评分模型。

不要做：

- 真实资金清算。
- 多学校运营。
- 原生 App 或小程序。
- 大而全的后台配置系统。
- 无测试的大型重构。

## 代码审查清单

每个 PR 只重点审查以下问题：

- 是否能跑通 `./scripts/check.sh`。
- 是否破坏既有 API 契约。
- migration 编号是否冲突，是否能在空库执行。
- 是否把密钥、个人配置或生成物提交进仓库。
- 登录态、认证态和管理员权限是否被绕过。
- 文件访问是否经过后端鉴权。
- 管理员访问敏感材料是否写入日志。
- 状态变化是否由后端决定，前端不能伪造关键状态。
- 是否有最小测试覆盖或可复现验证步骤。

## 每日收口规则

- 每天结束前必须合并一条更长的可演示链路。
- 晚上 21:00 后不再新增功能，只修 bug、补测试、补数据和补文档。
- 未合并的大分支必须拆小或降级。
- 如果出现冲突，以正式编号文档和契约 PR 为准。
- 如果某功能卡住超过 1 小时，先做降级实现，保留 TODO 或后续 PR。
