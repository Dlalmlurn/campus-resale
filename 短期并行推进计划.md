# 短期并行推进计划

评估日期：2026-05-25

## 当前口径

本计划基于当前 `main` 分支、M1 契约和已推进的 A/B/C 成员功能分支维护。

- M0 工程底座已完成：Spring Boot 后端、React/Vite 前端、PostgreSQL Flyway migration、MinIO、Docker Compose、基础测试脚本和 CI 已建立。
- M1 开发契约已合并到主线：`docs/阶段契约/m1_contracts.md` 是后续并行开发的共同接口和验收口径。
- A 成员身份、会话、权限基础能力已完成：注册、登录、退出、`/api/auth/me`、HttpOnly Cookie session、CSRF 最低防护、角色权限注解和演示账号均已落地。
- B 成员文件与校园认证能力已完成并合并：文件上传、认证提交、管理员审核、敏感材料访问留痕和完整 `canTrade` 规则已通过检查。
- C 成员商品目录能力已在 `feature/zike-goods-catalog` 完成并通过审查检查：商品草稿、图片关联、提交审核、管理员审核、公开列表、详情、关键词搜索、禁售词预检和少量可读占位图演示数据均已落地，等待后续 PR 合并或联调确认。
- 下一阶段主线是 M1：认证与商品 MVP，覆盖 `G01-G04` 的最小闭环。
- 四人都使用 Codex，开发速度不是主要瓶颈；主要瓶颈是接口契约一致性、代码审查、合并顺序和联调验收。
- 后续开发应优先保证每天都有可演示链路变长，而不是按模块堆半成品。

## 当前推进状态

更新日期：2026-06-01

| 成员 | 当前状态 | 说明 |
| --- | --- | --- |
| A | 已完成并进入后续集成口径 | 已实现 `/api/auth/register`、`/api/auth/login`、`/api/auth/logout`、`/api/auth/me`，提供 `CR_SESSION` HttpOnly Cookie、服务端 session、权限注解和演示账号。 |
| B | 已完成并合并 | 已实现 `/api/files/*`、`/api/verifications/*`、`/api/admin/verifications/*`，新增 `V6-V8` migration，接入完整交易权限规则。 |
| C | 已完成，待后续合并/联调 | 已实现 `/api/categories`、`/api/tags`、`/api/campus-places`、`/api/goods/*`、`/api/admin/goods/*`，新增 `V9-V11` migration，接入商品审核、搜索、禁售词预检和图片公开状态联动。 |
| D | 待继续联调 | 可基于 A/B/C 接口接入登录态、认证页、商品发布页、商品列表页、商品详情页和管理员审核页。 |

已验证：

- B 分支 `mvn test` 通过，后端 28 个测试全绿。
- B 分支空库按 `V1-V8` 顺序执行 migration 成功。
- B 分支 `./scripts/check.sh` 通过，包含后端测试、前端测试和前端构建。
- C 分支 `./scripts/check.sh` 通过，覆盖后端 33 个测试、前端测试和前端构建。
- C 分支临时 PostgreSQL 空库按 `V1-V11` 顺序执行 migration 成功，种子数据包含 4 个商品、3 个公开上架商品、4 条商品图片关联和 4 条可读占位图文件记录。

## M1 冲刺目标

目标日期：2026-05-26

M1 只锁定以下闭环：

```text
注册登录
  -> 提交校园认证
  -> 管理员审核认证通过
  -> 认证学生发布商品
  -> 管理员审核商品通过
  -> 商品进入公开列表、搜索和详情页
```

M1 当天不做以下内容：

- WebSocket 实时聊天。
- 订单、支付、退款、结算。
- AI 发布辅助。
- 复杂推荐。
- 评价信用。
- 举报申诉治理闭环。
- 公网 HTTPS 部署。

## 四人分工

| 成员 | 主线 | 后端交付 | 前端/联调交付 | 建议分支 |
| --- | --- | --- | --- | --- |
| A | 身份、会话、权限 | 注册、登录、退出、当前用户、HttpOnly Cookie、服务端 session、密码哈希、基础权限拦截、角色初始化。 | 提供接口示例和测试账号，协助前端接入登录态。 | `feature/member-auth-session` |
| B | 文件存储与校园认证 | MinIO 上传、文件元数据、认证提交、可信度计算、认证审核、敏感材料访问日志。 | 提供认证提交和审核 API 示例，协助认证页联调。 | `feature/member-campus-verification` |
| C | 商品、分类、审核、搜索 | 商品草稿、图片关联、提交审核、审核上架、商品列表、详情、关键词搜索、分类标签种子数据。 | 提供商品发布、审核、列表、详情 API 示例，协助商品页联调。 | `feature/member-goods-catalog` |
| D | 前端与集成验收 | 补充必要 BFF/接口适配建议，不主写复杂后端。 | 登录页、认证页、商品发布页、商品列表页、商品详情页、管理员审核页、路由守卫、API client、空状态和错误提示。 | `feature/member-m1-frontend` |

## 已合并的契约 PR

正式开发前的契约 PR 已完成：`feature/m1-contracts` 已合并。

该 PR 只做契约，不做业务实现，已固定：

- 契约文档：`docs/阶段契约/m1_contracts.md`
- API 路径前缀：
  - `/api/auth/*`
  - `/api/files/*`
  - `/api/verifications/*`
  - `/api/goods/*`
  - `/api/admin/*`
- 统一错误响应格式：
  - `code`
  - `message`
  - `details`
  - `traceId`
- 登录态返回结构：
  - `id`
  - `username`
  - `nickname`
  - `roles`
  - `verificationStatus`
  - `canTrade`
- M1 状态枚举：
  - 用户认证状态。
  - 文件用途和可见范围。
  - 商品状态。
  - 商品审核状态。
- migration 编号分配。
- M1 种子数据边界。

建议 migration 编号：

| 模块 | 编号范围 |
| --- | --- |
| Auth / Session / Roles | `V3-V5` |
| Files / Campus Verification | `V6-V8` |
| Goods / Categories / Audit | `V9-V12` |
| Search / Seeds / Integration Fixes | `V13-V14` |

## 当天合并顺序

1. `feature/m1-contracts`（已合并）
2. `feature/member-auth-session` / A 成员身份会话分支（已完成）
3. `feature/junjie-campus-verification` / B 成员文件与校园认证分支（已完成并合并）
4. `feature/zike-goods-catalog` / C 成员商品目录分支（已完成，待后续合并/联调）
5. `feature/member-m1-frontend`
6. `feature/m1-integration-polish`

合并原则：

- 小 PR 优先，单个 PR 只解决一个主题。
- 后端 PR 必须带基础测试或可复现验证命令。
- 前端 PR 必须能通过 `npm test -- --run` 和 `npm run build`。
- 每次合并前跑 `./scripts/check.sh`。
- 发现契约问题先改契约，再改实现，避免前后端互相猜。

## 2026-05-26 时间表

| 时间 | 目标 |
| --- | --- |
| 09:00-10:00 | 四人同步契约，完成 `feature/m1-contracts`。 |
| 10:00-13:00 | A/B/C 写后端主干，D 写前端路由、API client 和 mock 页面。 |
| 13:00-14:00 | 第一轮审查与合并：contracts + auth。 |
| 14:00-17:00 | B/C 完成认证和商品主流程，D 接入真实 auth。 |
| 17:00-18:00 | 第二轮审查与合并：files/verification + goods。 |
| 19:00-21:00 | 全链路联调：登录、认证、审核、发商品、审核商品、搜索浏览。 |
| 21:00-23:00 | 只修 bug、补测试、补种子数据、补 README，不加新功能。 |

## M1 验收清单

M1 完成时，至少可以演示：

- 用户注册、登录、退出。
- 登录态通过 HttpOnly Cookie 和服务端 session 维持。
- 用户可以查询当前登录状态。
- 普通用户可以提交校园认证资料。
- 文件二进制进入 MinIO，数据库保存文件元数据。
- 系统计算认证可信度。
- 管理员可以审核认证。
- 认证通过用户可以发布商品草稿并提交审核。
- 管理员可以审核商品。
- 审核通过商品进入公开列表。
- 公开商品支持详情查看和关键词搜索。
- 受保护接口不能被未登录用户直接访问。
- 业务验证和前端构建通过 `./scripts/check.sh`。

## A 成员身份会话实现约定

- 登录态使用 `CR_SESSION` HttpOnly Cookie，浏览器保存真实 session token，数据库 `user_sessions.session_token_hash` 只保存 `SHA-256(token)`。
- 用户名规则为 3 到 20 位，只允许字母、数字和下划线；服务端统一转小写入库，不允许空格和其他符号。
- 普通访问接口不轮换 token，只滑动续期服务端 `last_active_at` 和 `expires_at`；当前 Cookie `Max-Age` 仍按登录时写入的 7 天计算，不会随普通请求刷新。后续集成修正时需要把浏览器 Cookie 生命周期对齐到绝对 30 天或在有效请求后重写 Cookie。
- M1 最低 CSRF 防护采用 `Origin` / `Referer` 校验；它防普通跨站请求伪造，不防终端被控制或本站 XSS。
- 权限采用 `Filter` 识别当前用户、`Interceptor` + 注解执行登录和角色校验。
- 只有 `SUPER_ADMIN` 执行单端登录；`CONTENT_ADMIN` 允许多端登录，便于小组联调和课程演示。
- B 成员认证表合并前，`CurrentUser.canTrade` 暂按 `VERIFIED_STUDENT` 角色推导为 `true`；完整规则仍以认证分数、证件因子、审核状态和角色共同决定。
- 演示账号统一密码为 `520zikejiang`：`content_admin`、`super_admin`、`student_demo`、`user_demo`。

## B 成员文件与校园认证实现约定

- 文件二进制进入 MinIO，数据库 `stored_files` 只保存元数据。
- `CAMPUS_AUTH_MATERIAL` 在数据库和应用层都强制为 `ADMIN_ONLY`。
- 本人只能查看认证材料元数据和后端生成的脱敏预览，不能读取原始证件图片。
- 管理员读取认证材料原件时写入 `sensitive_access_logs`，`target_type = CAMPUS_AUTH_MATERIAL`。
- 校园认证表由 `V6__campus_auth_schema.sql` 创建，文件和敏感日志索引由 `V7__file_verification_indexes.sql` 补充，认证配置和 `student_demo` 已认证种子由 `V8__campus_auth_configs.sql` 补充。
- `PUT /api/verifications/me` 只保存草稿或积累状态，`POST /api/verifications/me/submit` 才进入审核队列。
- 提交审核必须已有学生证或校园卡材料。
- 校园邮箱 M1 只校验 `system_configs.campus.auth.email_suffixes` 后缀，默认 `example.edu`。
- `CurrentUser.canTrade` 已改为完整规则：认证分数至少 60、学生证或校园卡因子通过、认证状态 `APPROVED`，并且用户拥有 `VERIFIED_STUDENT` 角色。
- M1 不允许已 `APPROVED` 认证被原地重提并降级；后续若需要重新认证，应新增变更申请或版本模型。

## C 成员商品目录实现约定

- 商品模块包为 `backend/src/main/java/com/campusresale/goods`，沿用当前项目 `JdbcTemplate` / `NamedParameterJdbcTemplate` 风格。
- 商品发布和提交审核同时要求用户拥有 `VERIFIED_STUDENT` 角色，并且 B 的完整交易资格解析结果满足 `canTrade = true`。
- 商品草稿创建、编辑、提交审核、管理员通过、管理员驳回、我的商品、公开列表、公开详情和管理员审核队列接口已按 M1 契约落地。
- 商品审核驳回后设置 `status = DRAFT`, `auditStatus = REJECTED`，允许卖家修改后重新提交。
- 商品图片上传阶段保持 `PRIVATE`；审核通过后由 `syncGoodsImageVisibility` 统一开放为 `PUBLIC`；驳回、重新提交、非公开状态时由同一状态联动机制收回公开读取能力，不依赖人工记忆。
- 搜索使用 PostgreSQL `to_tsvector` + GIN，同时保留 `ILIKE` / `pg_trgm` 兜底，支持关键词、分类、价格、成色、地点和排序筛选。
- M1 已建 `audit_records`、`rule_hit_records`、`forbidden_terms`，并实现最小禁售关键词扫描；命中 `BLOCK` 规则时拒绝提交审核并记录规则命中。
- `V11` 已补少量演示商品种子：3 个公开上架商品和 1 个待审核商品。种子图片使用 `seed/goods-placeholder/` 前缀，由文件服务生成可读 PNG 占位图，真实用户上传商品图仍走 MinIO。
- C 分支已通过 `./scripts/check.sh`，并验证空库 `V1-V11` migration 成功。

## 后续推进节奏

M1 完成后，不再按原 M2-M5 横向铺模块，而是改为按“最小交易闭环 -> 后台验收闭环 -> 治理体验补丁”纵向推进。原 M2-M5 的能力不删除，而是拆入 N1-N3，优先保证系统能走完一条完整、可演示、可验收的校园二手交易路径。

核心原则：

- 下一阶段只服务一条完整演示路径，不能服务这条路径的功能全部后置。
- 先让系统成为“能完成交易的平台”，再补复杂协商、退款、治理、统计和体验增强。
- 每个阶段都必须产出可点击或可调用的闭环，不按模块堆半成品。

| 新阶段 | 建议时间 | 覆盖原阶段 | 目标 | 必须交付 |
| --- | --- | --- | --- | --- |
| N1 | 3-4 天 | M2 + M3 + M4 一部分 | 最小交易闭环 | 商品详情发起下单、订单创建、商品占用、卖家确认、买家模拟支付、面交完成确认、结算状态推进、最小评价。 |
| N2 | 2 天 | M5 + M4 一部分 | 后台验收闭环 | 管理员查看订单、支付、结算、评价、操作日志和基础统计；后台能证明关键状态变化可追溯。 |
| N3 | 2-3 天 | M4 + M5 剩余 | 治理体验补丁 | 举报、申诉、退款、通知、收藏、关注、信用摘要等按时间补强，优先做能支撑答辩演示的最小版本。 |
| 收尾 | 2-3 天 | 全阶段 | 验收与答辩准备 | 测试补齐、演示数据、README、部署说明、截图、答辩材料、bug bash。 |

## 后续成员分工

后续固定采用“领域主责 + 横向支援”的方式，不再让每个人各自铺一个大模块。推荐长期定位如下：

| 成员 | 固定定位 | 适合原因 |
| --- | --- | --- |
| A | 身份权限、安全审计、管理员边界 | A 已完成身份、会话、权限和 CSRF，后续所有交易、支付、后台接口都需要权限隔离和操作留痕。 |
| B | 文件证据、通知、认证/治理材料、敏感访问 | B 已完成文件、校园认证和敏感访问日志，退款证据、举报证据、申诉材料、通知都能复用这条能力线。 |
| C | 商品、订单、支付结算、评价的核心状态机 | C 已完成商品目录和商品状态联动，订单占用、支付成功、商品售出、结算推进都紧贴商品状态机。 |
| D | 前端、路由守卫、页面集成、演示验收 | D 负责把后端能力串成可点击闭环，并推动每个阶段形成可演示路径。 |

### N1 最小交易闭环分工

| 成员 | 主责 |
| --- | --- |
| A | 交易权限校验、买卖双方权限隔离、订单/支付接口登录保护、管理员操作权限。 |
| B | 最小通知，例如订单创建、卖家确认、支付成功、完成确认；文件证据接口先占位或简单实现。 |
| C | 订单表、订单状态机、商品行级锁占用、模拟支付、结算状态、最小评价和商品售出联动。 |
| D | 商品详情下单、我的订单、订单详情、模拟支付、完成确认、评价页面。 |

N1 可降级：

- 会话可以先不做 WebSocket，用订单备注、系统消息或普通接口支撑演示。
- 议价卡片可以先不做，订单金额直接来自商品挂牌价。
- 退款、举报、申诉先只保留入口或状态占位，不阻塞支付完成主链路。

### N2 后台验收闭环分工

| 成员 | 主责 |
| --- | --- |
| A | 管理员权限、操作日志、后台审计查询和账号/角色安全边界。 |
| B | 通知列表、敏感访问日志、文件/认证材料后台入口。 |
| C | 订单、商品、支付、结算和评价统计查询接口。 |
| D | 管理后台整合页面、统计页、日志页和演示导航。 |

N2 可降级：

- 后台配置类功能先不做大而全，只做能查看、能追踪、能解释状态变化。
- 统计先用简单 SQL 聚合或视图，不做复杂 BI。

### N3 治理体验补丁分工

| 成员 | 主责 |
| --- | --- |
| A | 处罚、限制交易、账号状态变更和管理员裁决权限。 |
| B | 举报、申诉、证据文件、敏感材料访问和治理审核记录。 |
| C | 退款、信用摘要、收藏/关注等与订单和商品状态相关的补强。 |
| D | 举报、申诉、退款、通知、收藏、信用等页面和最终体验 polish。 |

N3 可降级：

- 举报先覆盖商品和订单，不必立刻覆盖消息、账号等全部对象。
- 信用先做维度摘要和评价记录，不做复杂评分模型。
- 通知先做站内列表，不必做实时推送。

## 后续功能取舍

必须优先：

- 身份、认证、商品、订单、模拟支付、完成确认、结算和评价的主流程闭环。
- 状态机、幂等、审计、权限隔离。
- 可演示页面和可复现数据。
- 后台能查看关键状态和操作日志，便于答辩说明。

可以降级：

- WebSocket 可以先用轮询或普通接口补偿，最后再升级。
- 会话协商可以先弱化为订单备注或系统消息，不阻塞下单和支付。
- 退款、举报、申诉可以先做最小入口或状态占位，放到 N3 补强。
- AI 发布辅助可以保留开关和接口占位，不进入主路径。
- 推荐可以先做规则排序和关键词搜索，不做复杂个性化。
- 支付使用模拟渠道，不接真实支付平台。
- 信用展示先做维度摘要和记录列表，不做复杂评分模型。

不要做：

- 真实资金清算。
- 多学校运营。
- 原生 App 或小程序。
- 大而全的后台配置系统。
- 无测试的大型重构。

## 代码审查清单

每个 PR 只重点审查以下问题：

- 是否能跑通 `./scripts/check.sh`。
- 是否破坏既有 API 契约。
- migration 编号是否冲突，是否能在空库执行。
- 是否把密钥、个人配置或生成物提交进仓库。
- 登录态、认证态和管理员权限是否被绕过。
- 文件访问是否经过后端鉴权。
- 管理员访问敏感材料是否写入日志。
- 状态变化是否由后端决定，前端不能伪造关键状态。
- 是否有最小测试覆盖或可复现验证步骤。

## 每日收口规则

- 每天结束前必须合并一条更长的可演示链路。
- 晚上 21:00 后不再新增功能，只修 bug、补测试、补数据和补文档。
- 未合并的大分支必须拆小或降级。
- 如果出现冲突，以正式编号文档和契约 PR 为准。
- 如果某功能卡住超过 1 小时，先做降级实现，保留 TODO 或后续 PR。
