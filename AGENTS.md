## 1.全局约束
### 1.代码注释相关
后续工作的时候，记得加入代码注释增加可读性：代码加一下注解（开头解释每个整个代码文件作用，具体介绍里面每个模块或函数作用），不要有类似讲解提示这样的注解出现，不要跟着代码一行行加入，说明重要对象就行

### 2.NAS 适配相关
考虑到本项目主要部署在 NAS上，暴露在公网中，所以后续修改的时候请务必做好相关适配：Dockerfile 务必考虑到 caddy 相关。
NAS 是基于 debian 的下游发行版：飞牛。
端口相关：HTTPS 是 4343，HTTP 是 4342，尤其注意端口映射不要弄错。